import { AgeDirective } from './age.directive';

describe('AgeDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeDirective();
    expect(directive).toBeTruthy();
  });
});
