import { CheckFirstNameDirective } from './check-first-name.directive';

describe('CheckFirstNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckFirstNameDirective();
    expect(directive).toBeTruthy();
  });
});
